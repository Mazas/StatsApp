<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.WindowList"
        x:Class="WpfApp1.WindowList.ErrorLog"
        mc:Ignorable="d"
        Title="ErrorLog" Height="640" Width="860">
    <Grid>
        <ScrollViewer>
            <TextBox x:Name="LogText" Text="" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" Padding="0" Margin="5,20,5,5" IsReadOnly="True"/>
        </ScrollViewer>
        <DockPanel Grid.ColumnSpan="2" VerticalAlignment="Top">
            <Menu DockPanel.Dock="Top" Height="20">
                <MenuItem Header="_File">
                    <MenuItem Header="_Clear" Click="Clear"/>
                </MenuItem>
            </Menu>
        </DockPanel>
    </Grid>
</Window>
